# Enums — mini practice

Open each `.java` file under `src/main/java/learning/enums/practice/`; tasks are in the comment at the top of the file.

Run **`./run.sh`** when finished. Expected:

```text
BLACK
RED
```
