package learning.structure.util;

/*
 * Greeter
 *
 * This type lives in package learning.structure.util. The folder path under src mirrors that
 * (learning/structure/util/). Other packages import learning.structure.util.Greeter when they
 * need this public class. Non-public types in a file are only visible inside their own package.
 */
public class Greeter {

    public String hello(String name) {
        return "hello, " + name;
    }
}
