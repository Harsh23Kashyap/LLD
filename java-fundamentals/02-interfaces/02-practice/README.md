# Interfaces — mini practice

Open each `.java` file under `src/main/java/learning/oop/interfacespractice/`; the tasks for **that file** are in the block comment at the top.

Run **`./run.sh`** from this folder when finished (expect `waybill=WB-12` then `grams=1250`).
