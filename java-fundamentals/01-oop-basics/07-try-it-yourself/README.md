# Session 1 · Section 7 — Try it yourself

Short check before **`02-interfaces`**.

**Instructions:** Open each `.java` file in `src/main/java/learning/oop/practice7/` — the tasks for **that file** are in the comment block at the **top** of the file.

When you are done, from this folder run **`./run.sh`** (should print `A|150` then `B+Z|400` on separate lines).
